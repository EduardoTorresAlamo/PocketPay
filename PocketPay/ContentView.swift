//
//  ContentView.swift
//  PocketPay
//
//  Created by Eduardo Torres on 1/21/26.
//

import SwiftUI

/// Default Xcode-generated placeholder view.
///
/// This file is retained for reference but is no longer used as the app's
/// entry point. The active root view hierarchy starts in `PRPayApp` (App/PRPayApp.swift).
struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("Hello, world!")
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
